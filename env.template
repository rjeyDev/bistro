# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=baytown

# Check (receipt) printer - used when no printer is configured in Printers table (isKitchen=false)
# Network receipt printers often use raw port 9100
CHECK_PRINTER_IP=192.168.100.101
CHECK_PRINTER_PORT=9100
